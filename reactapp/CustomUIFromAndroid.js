'use strict';
import { PropTypes, requireNativeComponent } from 'react-native';
var iprop = {
    name: 'CustomUI',
//    propTypes: {
//        text: PropTypes.string
//    }
}
module.exports = requireNativeComponent('CustomUI', iprop);